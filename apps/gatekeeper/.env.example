# Copy to .env (never commit) for end-to-end coupon pushes.
# Bun auto-loads this file when running `make gatekeeper-add-coupon` or
# `make gatekeeper-sync-coupons` (the Makefile cd's into apps/gatekeeper).
CLOUDFLARE_API_TOKEN=
